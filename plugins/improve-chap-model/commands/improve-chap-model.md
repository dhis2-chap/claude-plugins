---
description: Iteratively improve an existing CHAP model, evaluating with the chap CLI.
---

Invoke the `improve-chap-model` skill and begin its workflow now.

If the user already mentioned a model (a local folder path or a GitHub URL), a
dataset (usually a local CSV), or an initial model configuration file
(YAML/JSON), pass those along to the skill. Otherwise the skill will ask for
them.

Do not reimplement the workflow here — the skill is the single source of truth.
