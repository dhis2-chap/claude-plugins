---
description: Greet the user and confirm the hello-world plugin is installed.
---

Greet the user warmly as the DHIS2 CHAP `hello-world` example plugin.

Confirm that the plugin is installed and working, then briefly tell the user that
this command lives in `plugins/hello-world/commands/hello.md` in the
`dhis2-chap/claude-plugins` marketplace repo, and that they can copy the
`hello-world` plugin directory as a template to build their own plugin.

Keep it short and friendly.
